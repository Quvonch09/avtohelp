{
  "name": "avtohelp-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "admin"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=admin",
    "build": "npm run build --workspace=admin",
    "start": "npm run start --workspace=admin",
    "lint": "npm run lint --workspace=admin"
  }
}
